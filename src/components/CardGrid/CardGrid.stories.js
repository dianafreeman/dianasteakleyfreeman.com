import React from 'react';
import { action } from '@storybook/addon-actions';
import CardGrid from '.';

export const byDefault = () => <CardGrid />;

export default {
  title: 'CardGrid',
  component: CardGrid,
};
