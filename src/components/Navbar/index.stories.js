import React from "react";
import NavBar from ".";

export const Main = (args) => {
  return <NavBar {...args} />;
};

export default {
  title: "Components/NavBar",
  component: NavBar,
  args: {},
};
